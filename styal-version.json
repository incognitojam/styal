{
  "$comment": "The next styal version to ship until a stable release reaches it; nightlies then preview the next patch after the newest stable tag. Change it only for a minor or major bump. Fork-only file, so the nightly rebase onto upstream never touches it.",
  "version": "0.1.0"
}
