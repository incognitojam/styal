# Two short recordings, about 3 minutes total

Thanks for helping. I'm testing dictation for a coding tool and need a few
different voices — mine alone isn't enough to trust the numbers.

**No software needed.** Use whatever records audio: Voice Memos, your phone's
recorder, WhatsApp voice note, anything. Any format is fine.

Please record **two separate files** and send both.

Two requests that matter more than they sound:

- **Read at a normal speaking pace.** People slow down and over-enunciate when
  recording. Careful speech is the easy case and won't tell me anything.
- **Don't restart if you stumble.** Trips, "um"s and self-corrections are useful
  — they're what real dictation sounds like.

---

## Recording 1

Read these programming terms **however feels natural to you**.
There's no right answer. Don't spell anything out.

> Read the worktreePath from the environmentId, then call parseURL on the apiUrl
> and pass the result to toJSON.
>
> The HTTPClient needs the userId before it can send the HTTPRequest. If the
> response isEmpty, call mapError on the FileSystem layer and check the exitCode.
>
> Run forEach over the rows, call toString on each one, and confirm the value
> isValid before writing createdAt and updatedAt. Remember getUser returns one row
> but getUsers returns many, and the thread_id lives next to the base64Encode
> helper in workspaceRoot.

---

## Recording 2

> The room is empty so please turn off the lights on your way out. It is valid to
> change your mind before the deadline, and I had to click through each item to
> check they were all correct.
>
> Map the error back to whoever reported it, then update the result and filter the
> list so the count comes out right. I called exit. The code path after that never
> runs.

Ordinary English, no programming names. This one checks the tool doesn't mangle
normal speech.

---

That's everything. Send both files back however's easiest.
