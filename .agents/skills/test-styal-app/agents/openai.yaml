interface:
  display_name: "styal App Testing"
  short_description: "Launch and retain isolated styal test environments"
  default_prompt: "Use $test-styal-app to launch an isolated styal environment and iteratively test it in the browser while preserving state."
