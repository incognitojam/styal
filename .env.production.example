# styal Link against the production Clerk instance and relay: the values CI
# bakes into the hosted app, releases, and previews. Copy to `.env` or
# `.env.local` only for local builds of those artifacts. Production Clerk does
# not accept sign-ins from localhost, so `vp run dev` needs
# .env.development.example instead. These are public identifiers, not secrets.
# Do not add server-side secrets to this file.
# See docs/operations/styal-link.md.

T3CODE_CLERK_PUBLISHABLE_KEY=pk_live_Y2xlcmsuc3R5YWwuYnVpbGQk
T3CODE_CLERK_JWT_TEMPLATE=t3-relay
T3CODE_CLERK_CLI_OAUTH_CLIENT_ID=WClDpS0H3fIS8m11
T3CODE_RELAY_URL=https://relay.styal.build
